package com.serverapp.enums;

public enum RequestType {
    SCREEN_CAPTURE,
    SYSTEM_INFO,
    PROCESS_LIST,
    COMMAND,
    PERFORMANCE_INFO,
    CONNECTION,
    EXIT_SCREEN_CAPTURE,
    EXIT_COMMNAD_SCREEN
}